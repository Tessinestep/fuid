document.addEventListener('DOMContentLoaded', (event) => {
  const leaderElement = document.getElementById('leaderElementId'); // Replace 'leaderElementId' with the actual ID
  if (leaderElement) {
    leaderElement.onclick = () => {
      // Code to be executed when the element is clicked
    };
  }
});
