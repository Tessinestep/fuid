const droppedItems = [
    { domain: 'example.com' },
    { domain: 'anotherdomain.net' },
    // ... more items
];

// Call the function to sort the dropped items
domainSortDropped('ascending'); // Sort in ascending order
domainSortDropped('descending'); // Sort in descending order
